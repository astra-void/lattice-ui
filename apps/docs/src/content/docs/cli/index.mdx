---
title: CLI Overview
description: Use the Lattice UI CLI to create projects and manage component dependencies.
draft: false
sidebar:
  order: 1
---

Audience: app developers integrating Lattice UI in project repositories.

The `lattice` CLI is the recommended way to scaffold and maintain Lattice dependencies.
It supports both interactive flows and explicit non-interactive runs.

## What the CLI Covers

- `create`: scaffold a new rbxts-based project
- `add`: install components by name and/or preset
- `remove`: remove selected component packages
- `upgrade`: update installed `@lattice-ui/*` packages
- `doctor`: validate lockfiles, peers, providers, and registry alignment

## Execution Modes

### Global CLI

```bash
npm i -g @lattice-ui/cli
lattice --help
```

### Monorepo Local Script

```bash
pnpm lattice --help
```

## Learn the Workflow

- Full command signatures: [CLI Commands](/cli/commands/)
- Registry component names and presets: [Components and Presets](/cli/components-and-presets/)
- Interactive and output model: [Execution and Output](/cli/execution-and-output/)
- Failure recovery patterns: [Troubleshooting](/cli/troubleshooting/)
